{
  "name": "nan-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Untrusted public TCP relay for NAN enclaves — SNI-routes client TLS to the enclave's /x/:id/tls/:port bridge. Holds no keys, sees only ciphertext.",
  "main": "relay.js",
  "scripts": {
    "start": "node relay.js",
    "start:api": "node api-relay.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "viem": "^2.53.1",
    "ws": "^8.21.0"
  }
}
